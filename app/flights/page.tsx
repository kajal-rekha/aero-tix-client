import Flights from "@/components/Flights";
import React from "react";

const FlightsPage = () => {
    return (
        <main>
            <Flights />
        </main>
    );
};

export default FlightsPage;
